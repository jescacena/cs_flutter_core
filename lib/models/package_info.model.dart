class CS_PackageInfo {
  String? appName;
//  = packageInfo.appName;
  String? packageName;
//  = packageInfo.packageName;
  String? version;
// = packageInfo.version;
  String? buildNumber;
//  = packageInfo.buildNumber;

  CS_PackageInfo({this.appName, this.packageName, this.version, this.buildNumber});
}
